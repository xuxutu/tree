传统数组的缺点：

    1.数组长度必须事先制定，且只能是常整数，不能是变量

    2.函数内定义数组，程序员无法手动释放，只有函数结束时才得以释放。

    3.数组的长度不能动态地改变。

    4.不能跨函数使用。


动态分配内存：

    int * p=(int *)malloc(4);

    free（p）p指向的内存释放掉；告诉操作系统，这块内存我不用了

    内存中开辟四个字节，返回第一个字节的首地址，需要强制转换。
  