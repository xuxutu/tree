1.if  else
  if(1)     // 如果1成立执行A。
     A;
  else if(2)// 如果1不成立，2成立，执行B。
     B;
  else if(3)// 如果1，2不成立，3成立，执行C。
     C;
  else      //如果1、2、3都不成立，执行D.
     D;
  if else 中后只能跟一个语句。

2.
  
  if (3>2);//空语句 等价于：

  IF（3>2）
          ;